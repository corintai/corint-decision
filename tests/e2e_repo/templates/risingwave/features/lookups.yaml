features:
  - name: rw_txn_count_1h
    type: lookup
    datasource: risingwave_e2e
    key: "${event.user.id}"
    # Missing rows and query errors both require review in this test policy.
    fallback: -1
