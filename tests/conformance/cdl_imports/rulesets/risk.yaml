version: "0.1"
import:
  rules: [rules/amount.yaml]
---
ruleset:
  id: risk
  rules: [large_amount]
  conclusion:
    - when: total_score >= 60
      signal: decline
    - when: total_score > 50
      signal: review
    - default: true
      signal: approve
