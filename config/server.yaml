# CORINT Decision Engine Server Configuration

# Server host (127.0.0.1 for localhost only, 0.0.0.0 for all interfaces)
host: "127.0.0.1"

# Server port
port: 8080

# Rules directory
# All .yaml and .yml files in this directory will be loaded automatically
rules_dir: "examples/pipelines"

# Enable metrics collection
enable_metrics: true

# Enable distributed tracing
enable_tracing: true

# Log level (trace, debug, info, warn, error)
log_level: "info"

# Database URL for decision result persistence (optional)
# If not set, decision results will not be persisted to database
# You can also set DATABASE_URL environment variable as an alternative
# Example: postgresql://user:password@localhost:5432/corint_risk
# database_url: "postgresql://postgres:password@localhost:5432/corint_risk"
database_url: "postgresql://postgres.hfsbvqfkwbslcuvokmis:XPRozJ3DZox5KUnh@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require"